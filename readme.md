my_flask_app/
│
├── app.py              # The entry point for your Flask app (Controller)
├── models/             # Folder for models (business logic / data)
│   └── __init__.py     # Initializes the models package
│   └── user.py         # Example model
├── templates/          # Folder for HTML templates (Views)
│   └── index.html      # Home page template
├── static/             # Folder for static files like CSS, JS, images
│   └── style.css       # Example CSS file
└── config.py           # (Optional) Configuration file for settings
